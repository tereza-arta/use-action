name: Test

on: [push]

jobs:
  get-message:
    name: A job to get message
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
     
      - name: pwd and ls
        run: |
          pwd
          ls
          
      - name: Write in new file
        run: echo ${{ secrets.SOME }} > file.txt
      
